/* =========================================================================
   ccmw — Textual CSS (styles.tcss)
   Dark theme: surface background, accent highlights
   ========================================================================= */

/* -------------------------------------------------------------------------
   Screen / Root
   ------------------------------------------------------------------------- */

Screen {
    background: $background;
    layout: vertical;
}

/* -------------------------------------------------------------------------
   Main container — horizontal split: FileBrowser(18%) + FileViewer(42%) + TerminalPanel(1fr)
   ------------------------------------------------------------------------- */

#main-container {
    layout: horizontal;
    width: 100%;
    height: 1fr;
}

/* -------------------------------------------------------------------------
   FileBrowser pane — Vertical wrapper (width + border 소유)
   ------------------------------------------------------------------------- */

#file-browser-pane {
    width: 18%;
    height: 100%;
    border: solid $surface-lighten-2;
    background: $surface;
    scrollbar-color: $accent-darken-2;
    scrollbar-color-hover: $accent;
    scrollbar-color-active: $accent-lighten-1;
}

#file-browser-pane:focus-within {
    border: solid $accent;
}

/* -------------------------------------------------------------------------
   상태바 (현재 경로 + 한/영 표시)
   ------------------------------------------------------------------------- */

#status-bar {
    width: 100%;
    height: 1;
    background: $surface-lighten-1;
}

#current-dir {
    width: 1fr;
    height: 1;
    content-align: left middle;
    padding: 0 1;
    color: $text-muted;
    overflow: hidden hidden;
    text-wrap: nowrap;
}

#git-branch {
    width: auto;
    height: 1;
    content-align: center middle;
    padding: 0 1;
    color: $text-muted;
}

#git-branch.git-dirty {
    color: yellow;
}

#git-branch.git-clean {
    color: $success;
}

#input-lang {
    width: 4;
    height: 1;
    content-align: center middle;
    text-style: bold;
}

#input-lang.lang-ko {
    background: #c0392b;
    color: #ffffff;
}

#input-lang.lang-en {
    background: $accent;
    color: $background;
}

/* -------------------------------------------------------------------------
   FileBrowser widget
   ------------------------------------------------------------------------- */

#file-browser {
    width: 100%;
    height: 1fr;
    background: $surface;
}

FileBrowser {
    background: $surface;
}

FileBrowser > .tree--cursor {
    background: $accent-darken-2;
    color: $text;
}

FileBrowser:focus > .tree--cursor {
    background: $accent;
    color: $text;
}

FileBrowser .tree--highlight {
    background: $surface-lighten-1;
}

/* -------------------------------------------------------------------------
   FileViewer panel — 가로/세로 스크롤 모두 활성화
   ------------------------------------------------------------------------- */

#file-viewer {
    display: none;
    width: 42%;
    height: 100%;
    border: solid $surface-lighten-2;
    background: $surface-darken-1;
    scrollbar-color: $accent-darken-2;
    scrollbar-color-hover: $accent;
    scrollbar-color-active: $accent-lighten-1;
}

#main-container.viewer-open #file-viewer {
    display: block;
}

#main-container.viewer-open #terminal-panel {
    width: 40%;
}

#file-viewer:focus-within {
    border: solid $accent;
}

FileViewer {
    background: $surface-darken-1;
    overflow-x: auto;
    overflow-y: auto;
}

FileViewer > #viewer-title {
    width: 100%;
    height: 1;
    background: $surface-lighten-1;
    color: $text-muted;
    padding: 0 1;
    text-style: bold;
}

FileViewer > #viewer-scroll {
    width: 1fr;
    height: 1fr;
    padding: 0 1;
    overflow-x: auto;
    overflow-y: auto;
}

FileViewer > #viewer-scroll > #file-content {
    width: auto;
    background: $surface-darken-1;
}

FileViewer.word-wrap-on > #viewer-scroll > #file-content {
    width: 1fr;
}

/* -------------------------------------------------------------------------
   TerminalPanel / MultiTerminalPanel panel
   ------------------------------------------------------------------------- */

#terminal-panel {
    width: 1fr;
    height: 100%;
    border: solid $surface-lighten-2;
    background: $surface-darken-1;
    scrollbar-color: $accent-darken-2;
    scrollbar-color-hover: $accent;
    scrollbar-color-active: $accent-lighten-1;
}

#terminal-panel:focus-within {
    border: solid $accent;
}

MultiTerminalPanel {
    background: $surface-darken-1;
}

MultiTerminalPanel TabbedContent {
    background: $surface-darken-1;
}

MultiTerminalPanel TabPane {
    padding: 0;
    background: $surface-darken-1;
}

TerminalPanel {
    background: $surface-darken-1;
}

TerminalPanel > #terminal-output {
    width: 1fr;
    height: 1fr;
    background: $surface-darken-1;
    color: $text;
    text-wrap: nowrap;
    overflow: hidden hidden;
}

/* -------------------------------------------------------------------------
   SessionPanel — bottom panel
   ------------------------------------------------------------------------- */

#session-panel {
    width: 100%;
    height: auto;
    max-height: 14;
    border: solid $surface-lighten-2;
    background: $surface;
    layer: base;
}

#session-panel:focus-within {
    border: solid $accent;
}

#session-list-title {
    background: $surface-lighten-1;
    color: $accent;
    text-style: bold;
    padding: 0 1;
    width: 100%;
}

#session-list-view {
    height: 1fr;
    background: $surface;
    scrollbar-color: $accent-darken-2;
    scrollbar-color-hover: $accent;
}

#session-list-view > ListItem {
    background: $surface;
    padding: 0 1;
    border-left: thick $surface;
}

#session-list-view > ListItem:hover {
    background: $surface-lighten-1;
}

#session-list-view > ListItem.--highlight {
    background: $surface-lighten-2;
    color: $text;
    text-style: bold;
    border-left: thick $surface-lighten-2;
}

#session-list-view > ListItem.--highlight > Label {
    background: $surface-lighten-2;
    color: $text;
    text-style: bold;
    width: 100%;
}

#session-list-view:focus > ListItem.--highlight {
    background: $accent;
    border-left: thick $accent-lighten-2;
}

#session-list-view:focus > ListItem.--highlight > Label {
    background: $accent;
    color: $background;
    text-style: bold;
    width: 100%;
}

/* -------------------------------------------------------------------------
   GitPanel — bottom panel
   ------------------------------------------------------------------------- */

#git-panel {
    width: 100%;
    height: 35;
    max-height: 35;
    border: solid $surface-lighten-2;
    background: $surface;
    layer: base;
}

#git-panel:focus-within {
    border: solid $accent;
}

#git-panel-title {
    background: $surface-lighten-1;
    color: $accent;
    text-style: bold;
    padding: 0 1;
    width: 100%;
    height: 1;
}

#git-panel-body {
    width: 100%;
    height: 1fr;
}

#git-panel-left {
    width: 36;
    height: 100%;
    border-right: solid $surface-lighten-2;
}

#git-file-list {
    height: 1fr;
    background: $surface;
    scrollbar-color: $accent-darken-2;
    scrollbar-color-hover: $accent;
}

#git-file-list > ListItem {
    background: $surface;
    padding: 0 1;
    border-left: thick $surface;
}

#git-file-list > ListItem:hover {
    background: $surface-lighten-1;
}

#git-file-list > ListItem.--highlight {
    background: $surface-lighten-2;
    border-left: thick $surface-lighten-2;
}

#git-file-list:focus > ListItem.--highlight {
    background: $accent-darken-2;
    border-left: thick $accent;
}

/* -------------------------------------------------------------------------
   변경사항 / 브랜치 뷰 전환 컨테이너
   ------------------------------------------------------------------------- */

#changes-view {
    width: 100%;
    height: 1fr;
    layout: vertical;
}

#branches-view {
    width: 100%;
    height: 1fr;
    layout: vertical;
}

/* -------------------------------------------------------------------------
   브랜치 목록
   ------------------------------------------------------------------------- */

#branch-list {
    height: 1fr;
    background: $surface;
    scrollbar-color: $accent-darken-2;
    scrollbar-color-hover: $accent;
}

#branch-list > ListItem {
    background: $surface;
    padding: 0 1;
    border-left: thick $surface;
}

#branch-list > ListItem:hover {
    background: $surface-lighten-1;
}

#branch-list > ListItem.--highlight {
    background: $surface-lighten-2;
    border-left: thick $surface-lighten-2;
}

#branch-list:focus > ListItem.--highlight {
    background: $accent-darken-2;
    border-left: thick $accent;
}

#branch-name-input {
    width: 100%;
    height: 3;
    margin: 0;
    background: $surface-darken-1;
    border: tall $surface-lighten-2;
}

#git-branch-buttons {
    width: 100%;
    height: 3;
    padding: 0 1;
}

#git-branch-buttons Button {
    margin: 0 0 0 1;
    width: 1fr;
}

/* -------------------------------------------------------------------------
   커밋 입력 / 버튼 행
   ------------------------------------------------------------------------- */

#git-commit-msg {
    width: 100%;
    height: 3;
    margin: 0;
    background: $surface-darken-1;
    border: tall $surface-lighten-2;
}

#git-stage-row {
    width: 100%;
    height: 3;
    align: right middle;
    padding: 0 1;
}

#git-stage-row Button {
    margin: 0 0 0 1;
    min-width: 10;
}

#git-remote-row {
    width: 100%;
    height: 6;
    padding: 0 1;
    layout: vertical;
}

#btn-pull {
    width: 100%;
    height: 3;
    margin: 0;
}

#btn-push {
    width: 100%;
    height: 3;
    margin: 0;
}

#btn-review {
    width: 100%;
    height: 3;
    margin: 0;
}

#btn-git-init {
    width: 100%;
    height: 3;
    margin: 0;
}

#git-diff-pane {
    width: 1fr;
    height: 100%;
}

#git-diff-title {
    width: 100%;
    height: 1;
    background: $surface-lighten-1;
    color: $text-muted;
    padding: 0 1;
    text-style: bold;
}

#git-diff-unified {
    width: 100%;
    height: 1fr;
    background: $surface-darken-1;
    padding: 0 1;
    scrollbar-color: $accent-darken-2;
    scrollbar-color-hover: $accent;
}

#git-diff-split {
    width: 100%;
    height: 1fr;
}

#git-diff-old {
    width: 1fr;
    height: 100%;
    background: $surface-darken-1;
    padding: 0 1;
    border-right: solid $surface-lighten-2;
    scrollbar-size: 0 0;
}

#git-diff-new {
    width: 1fr;
    height: 100%;
    background: $surface-darken-1;
    padding: 0 1;
    scrollbar-color: $accent-darken-2;
    scrollbar-color-hover: $accent;
}

/* -------------------------------------------------------------------------
   Utility
   ------------------------------------------------------------------------- */

.hidden {
    display: none;
}

/* -------------------------------------------------------------------------
   Header / Footer
   ------------------------------------------------------------------------- */

Header {
    background: $surface-lighten-1;
    color: $accent;
    text-style: bold;
    dock: top;
    height: 1;
}

Footer {
    background: $surface-lighten-1;
    color: $text-muted;
    dock: bottom;
    height: 1;
}

Footer > .footer--key {
    color: $accent;
    text-style: bold;
    background: $surface-darken-1;
}

Footer > .footer--description {
    color: $text-muted;
}
